# Production Environment Variables
# Copy this to Render Dashboard → Environment Variables

# CRITICAL: Generate secure secret key
# Run: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-generated-production-secret-key-min-32-chars

# Application Settings
DEBUG=false
LOG_LEVEL=INFO
API_V1_STR=/api/v1

# Server Configuration
SERVER_NAME=GMet Weather API
SERVER_HOST=https://gmet-weather-api.onrender.com

# CORS - Update with your actual frontend URLs
# Comma-separated list (no spaces)
BACKEND_CORS_ORIGINS=https://your-frontend.com,https://dashboard.yourapp.com

# Database Configuration (Render auto-fills these from PostgreSQL service)
POSTGRES_SERVER=${POSTGRES_HOST}
POSTGRES_PORT=${POSTGRES_PORT}
POSTGRES_USER=${POSTGRES_USER}
POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
POSTGRES_DB=${POSTGRES_DATABASE}

# JWT Settings
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080  # 7 days

# Rate Limiting
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60  # seconds

# Weather Data Settings
WEATHER_DATA_RETENTION_DAYS=365

# Optional: Redis for Caching (if added)
# REDIS_HOST=redis-service.railway.internal
# REDIS_PORT=6379
# REDIS_PASSWORD=
# REDIS_DB=0

# Optional: External Weather APIs (future)
# OPENWEATHER_API_KEY=your-openweather-key
