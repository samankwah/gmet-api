# Production Requirements - GMet Weather API
# Excludes development/testing packages

# Web Framework
fastapi==0.115.6  # Upgraded for Pydantic 2.10 compatibility
uvicorn[standard]==0.34.0  # Upgraded for better stability

# Database
sqlalchemy[asyncio]==2.0.23
asyncpg==0.30.0  # Upgraded for Python 3.13 compatibility (has pre-built wheels)
psycopg2-binary==2.9.9  # Required for sync PostgreSQL migrations with Alembic
alembic==1.12.1
python-dateutil==2.9.0

# Pydantic & Settings
pydantic==2.10.3  # Upgraded for better wheel support (Python 3.11-3.13)
pydantic-settings==2.6.1
email-validator==2.3.0  # Required for pydantic EmailStr validation

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.1.2
python-multipart==0.0.6

# CORS & Middleware
python-dotenv==1.0.0
slowapi==0.1.9

# Production server
gunicorn==21.2.0

# NOTE: pandas and openpyxl removed from production
# They're only needed for import scripts, which can use full requirements.txt locally
