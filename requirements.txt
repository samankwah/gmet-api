# Web Framework
fastapi==0.115.6  # Upgraded for Pydantic 2.10 compatibility
uvicorn[standard]==0.34.0  # Upgraded for better stability

# Database
sqlalchemy[asyncio]==2.0.36  # Upgraded for Python 3.13 compatibility
asyncpg==0.30.0  # Upgraded for Python 3.13 compatibility (has pre-built wheels)
psycopg[binary]==3.2.4  # psycopg3 - Python 3.13 compatible sync driver for Alembic
aiosqlite==0.19.0  # For SQLite support in development
alembic==1.14.0  # Upgraded for compatibility with SQLAlchemy 2.0.36
python-dateutil==2.9.0  # Required for alembic timezone support

# Pydantic & Settings
pydantic==2.10.3  # Upgraded for better wheel support (Python 3.11-3.13)
pydantic-settings==2.6.1
email-validator==2.3.0  # Required for pydantic EmailStr validation

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0  # Pin bcrypt to 4.x for passlib compatibility
python-multipart==0.0.6

# CORS & Middleware
python-dotenv==1.0.0
slowapi==0.1.9  # Rate limiting

# Development & Testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2

# Data Processing
pandas==2.2.0
openpyxl==3.1.2  # For Excel file support

# Redis for caching
redis==5.0.1

# Production server
gunicorn==21.2.0  # Production WSGI server

# Code Quality
black==23.11.0
isort==5.12.0
flake8==6.1.0
